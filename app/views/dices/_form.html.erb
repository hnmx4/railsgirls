<%= form_for(@dice) do |f| %>
  <% if @dice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dice.errors.count, "error") %> prohibited this dice from being saved:</h2>

      <ul>
      <% @dice.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :picture %><br />
    <%= f.text_field :picture %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :comment %><br />
    <%= f.file_area :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
